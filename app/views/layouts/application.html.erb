<!DOCTYPE html>
<html>
<head>
  <title>Payrolles</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<style>
 a img {
border: none !important;
display: block;
border-bottom: 0px;
}
a:hover img {
border: none !important;
display: block;
border-bottom: 0px;
}
.menu ul { padding:0; margin:0; list-style:none; border:0; float:left;}
.menu ul li { float:left; margin:0; padding:0; border:0;}
.menu ul li a { float:left; margin:0; padding:0px  07px;  text-decoration:underline; border:1px solid #000;}
</style>
<body>
<!--<div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="#">Project name</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li class="active"><a href="#">Home</a></li>
              <li><a href="#about">About</a></li>
              <li><a href="#contact">Contact</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>-->
<div id="user_nav" class="navbar navbar-inverse navbar-fixed-top" >
  <% if current_user %>
  <div class="container" style="width:100%">
    <div style="float:left;">
      <%= image_tag("Logo-website2.png",:class => 'aimg',:style => "background-color:white;height:60px") %>
    </div>  
    <div class="menu" style="float:left;width:50%;color:white;margin:20px 40px 0px 50px;">
     <ul> 
      <li> <a href="<%= employes_path %>" class="<%= controller_name == 'employes' ? 'btn-info'  : '' %> btn-default " style="font-size: 13pt;">Manage Employees</a></li>  
      <li> <a href="<%= leaves_path %>" class="<%= controller_name == 'leaves' ? 'btn-info'  : '' %> btn-default" style="font-size: 13pt;">Manage Leaves</a></li>
      <% if current_user.is_admin? %>
        <li> <a href="<%= holidays_path %>" class="<%= controller_name == 'holidays' ? 'btn-info'  : '' %> btn-default" style="font-size: 13pt;">Manage Holidays</a></li>
      <% else %>  
         <li> <a href="<%= holidays_path %>" class="<%= controller_name == 'holidays' ? 'btn-info'  : '' %> btn-default" style="font-size: 13pt;">Holidays Calender</a></li>
      <% end %>
    </ul>
    </div>
    <div style="float:left;">
    <%= link_to image_tag("home2.png",:class => 'aimg',:style => "float:right;height:40px;margin:10px;"), root_path %>
    </div>
    <div style="float:left;color:white;font-size: 12pt;margin:20px 10px 0px 0px;">
     Logged in as <%= current_user.first_name %> &nbsp;|&nbsp; 
      <%= link_to "Log out", destroy_user_session_path, :method => 'delete' %>
    </div>
  </div>
      
  <% else %>
    <%= image_tag("Logo-website2.png",:class => 'aimg',:style => "background-color:white;height:60px") %>
    <div style="float:right;color:white;font-size: 12pt;margin:15px">
     <%= link_to "Sign up..", sign_up_path %> or
     <%= link_to "log in..", log_in_path %>
    </div>
  <% end %>
</div>
</br>
</br>
</br>
</br>
</brr>
<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>

<%= yield %>

</body>
</html>

